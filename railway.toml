[deploy]
startCommand = "flask db upgrade && flask seed-auto && gunicorn --workers 2 --bind 0.0.0.0:$PORT --timeout 120 run:app"
healthcheckPath = "/auth/login"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
